
import {
     IUser, API_URL, showAlertMessage, IChatParticipant, Search, ScrollableList, showInternal, getRandomArbitrary
    , extractCommonFields
} from "../../../../../shared";


interface bebra extends IUser {
    a: number
}
export const PickButton = () => {
    TablerIcon
    IUser
    API_URL
    Search
    ScrollableList
    IChatParticipant
    showInternal
    extractCommonFields()
    getRandomArbitrary(1, 2, 3, 4)


    return (<Search key={1} />)
}

// const arr = [1,2,3,4,5]

// const copy = JSON.parse(JSON.stringify(arr))